import React from "react";
import ReactDOM from "react-dom/client";
import PopupTab from "./popup-tab.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PopupTab />
  </React.StrictMode>
);
